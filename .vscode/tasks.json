{
    "tasks": [
        {
            "type": "shell",
            "label": "gcc-8 build active file",
            "command": "/usr/local/bin/gcc-8",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "/usr/local/bin"
            }
        }
    ],
    "version": "2.0.0"
}